import { UploadForm } from "@/components/UploadForm";

export default function UploadPage() {
  return (
    <section className="px-4 py-8 sm:py-12 flex-1 flex justify-center">
      <UploadForm />
    </section>
  );
}
